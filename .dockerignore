node_modules
dist
.env
.env.*
secrets.env
secrets.env.tpl
*.db
.git
.github
.gitignore
.dockerignore
*.md
tests
# Exclude host-side tooling but keep build-time scripts in the context.
# (A bare "scripts" dir rule would prune the tree and make the negation useless.)
scripts/*
!scripts/copy-static.mjs
docker-compose.yml
drizzle.config.ts
vitest.config.*
.vscode
